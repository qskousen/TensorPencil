- [FIXED 2026-07-17] vulkan qwen3-4B autoregressive *generation* produced garbage (`较长较长…`). ROOT CAUSE: the Vulkan dense matmul (`opMatmul`/`pipe_f32`) and decode-GEMV (`opGemv`/`opGemvPartial`/`opGemvPartial4`) kernels only handled 1-byte (fp8) and 4-byte (f32) weights — there was NO bf16 weight path. qwen3 CausalLM checkpoints are bf16, so `dtype_f8=false` routed them to `pipe_f32`/`weightBuffer(esize=4)`, reinterpreting the 2-byte bf16 weights as f32 → garbage. The diffusion *encode* path worked only because its checkpoint is fp8 (1-byte `pipe_f8`), and the existing `vulkan spec decode` test used that fp8 model AND only compared Vulkan-to-Vulkan, so the bf16 path was never exercised. FIX = **native bf16 GEMV** (mirrors CUDA `gemv_bf16`): new `context.WCode` enum (f32/f8/bf16, int value = kernel dtype flag) replaces the `dtype_f8` bool on the three GEMV wrappers (only `qwen3_gpu` calls them, so contained; `opMatmul` kept its bool); new `transpose_bf16` kernel (2 cols/u32, k-major) + `pipe_tr_bf16` so `weightBuffer(esize=2)` packs bf16; bf16 branch in `gemv_partial`/`gemv_partial4` (u16 load → `<<16` bf16→f32 in-register). bf16 has no tiled GEMM so `VulkanLM.gemm` streams bf16 prefill through grouped GEMV (weight read ceil(m/4)×, like CUDA `opGemvBf16N`). Weights stay 2-byte. VALIDATED on 3090 (4B bf16): coherent vs CPU, **31.7 tok/s**, **~8GB weights** (ctx auto-sizes 32768, ~10.9GB total incl KV). Not token-identical (GEMV reduction-order drift; fine — correctness-first). Regression test `vulkan bf16 prefill argmax matches cpu` in qwen3_gpu.zig (CPU-vs-Vulkan, bf16 model); fp8 spec test still passes. (A first-cut f32-widen approach worked but cost 2× VRAM + half the speed, so it was replaced.) Perf headroom: grouped-GEMV prefill re-reads the weight ceil(m/4)×; a real bf16 tiled GEMM (new SPIR-V module) would read once.
- zig-cuda backend seems to have oom issues
- the thinking toggle is not shown until the model loads, which means you can't toggle it before the first message - also can't attach images as the first message
- backend switch in queue doesn't switch backend it seems like? is backend attached to images in the queue?
- begin filling in holes in the capabilities grid (BACKEND.md)
- add more sampling methods
- tool call boxes inline, expandable to see the tool call
- chat ui touchup because it's super ugly
- GPU sampling follow-ups (phases 1-4 landed: on-device argmax + top-k for qwen3 on all backends, chain-greedy spec verify):
  - spec-decode TREE-greedy verify on GPU: generateTree still downloads (1+m)*vocab (up to ~38MB for a 64-node tree). Add CudaLM.stepAllTreeArgmax (tree forward + per-row opArgmax) and have the greedy walk use g[cur] instead of sampler.dist(row).ids[0] (gated on repeat_penalty==1.0). Needs an EAGLE draft head to validate e2e.
  - spec-decode STOCHASTIC-chain verify on GPU: reuse opTopK per row + rebuild the Dist on the CPU (accept/sampleExcluding), so temp>0 --spec-k stops downloading (k+1)*vocab.
  - device repetition-penalty kernel: the GPU argmax/top-k/stepSelect paths fall back to the full CPU download whenever repeat_penalty != 1.0 (needs the full logits). A tiny single-thread scatter kernel over the recent window (sequential, matches the CPU compounding order) would keep rep-penalty on the GPU path.
  - fan out on-device sampling: qwen3 (all), qwen35_cuda, gemma3_cuda DONE (stepArgmax/stepSelect/maxSelect; greedy validated token-identical to CPU on 9B/12B). REMAINING: (a) gemma4_cuda needs a device finalizeLogits kernel (tanh softcap + suppress-token masking is applied host-side after download, so device logits aren't final — argmaxing them could pick a suppressed token); until then gemma4 stays on the CPU-sample download path. (b) Vulkan steppers qwen35_gpu/gemma3_gpu (single-row, no per-row-offset issue — straightforward mirror of qwen3_gpu, just lower priority since Vulkan LLM is correctness-first/slow).
  - Vulkan stepAllArgmax: needs a per-row offset in the argmax kernel (opElt binds whole buffers); skipped for now (lower priority — Vulkan qwen3 generation now works but is correctness-first/slow; the bf16-garbage bug is fixed, see top).
- LLM KV-cache memory footprint (why gemma4-12B grows ~0.4-1GB/msg on cuda in tp-gui; the VRAM meter is honest — Session.ctxKvBytes = cached_tokens * Σ_layers kvDim(l) * 8 (K+V, f32); ~7000 cached tokens on a 12B = ~5.4GB. NOT a leak, NOT re-prefilling the system prompt (engine.generate only prefills ids[cached()..]; the cache is reused across turns). Ordered biggest-lever-first:
  - (1, biggest) [DONE on GPU backends 2026-07-17; pure-CPU pending] Bound LOCAL (sliding-window) layer KV to a fixed ring instead of full context. gemma3/gemma4 are ~5:1 local:global, so ~5/6 of layers allocate the whole context but only ever READ within the window. Made those layers a ring of `sliding_window + prefill_chunk` rows (the +chunk slack keeps a prefill batch from aliasing a still-needed key) → their KV plateaus instead of growing. Ring size decouples storage row (`pos % ring`) from the absolute position (RoPE still uses absolute). Storage-only change; token-identical by construction (only drops rows the window mask already excludes). DONE: gemma4 cuda/zig-cuda (A/B token-identical, runs at full 131072 ctx where old OOMs), gemma3 cuda/zig-cuda (A/B token-identical) incl. the CPU-offload split (ring↔linear translation in migrate/promote, `ringSegments` unit-tested), gemma3 vulkan (coherent; `attn_dsplit_gemma` split attention added), ctxKvBytes meter. PENDING: pure-CPU (gemma3.zig/gemma4.zig) — needs the reference forward's prefill chunked (it currently does the whole prompt in one pass) + a PerLayerKvCache ring; host-RAM only, deferred as lower priority. Each backend has an `enable_local_ring` compile kill-switch.
  - (2) f16 KV cache instead of f32 (halves footprint across ALL models/backends). Selectable dtype (`--kv-dtype f16` / tp-gui "KV cache dtype" dropdown); `kv_cache.KvDtype` carried on `Capacity` to every model init. [gemma4 CUDA DONE+validated 2026-07-17: new `attn_split_h256_f16`/`attn_split_h512_f16` PTX (native f16 K/V loads, q/out stay f32), `opStoreKvF16` convert-on-store, `opAttnDecode` kv_f16 flag, `reinitCache`+`Session.rebuildContext` (GUI toggle = context reload, weights stay resident), GPU unit test vs f32 ref ≤5e-3. gemma4-12B: coherent (f32-identical on deterministic prompts), ~350MB less at ~1350 tok, 52 tok/s. Lossy → NOT token-identical in general.] ALL CUDA ARCHS DONE+validated 2026-07-17 (gemma4/gemma3/qwen3 hd128/qwen35 hd256): added `attn_split_f16` (hd128) + graph-capture `attn_split_s_f16`/`attn_split_h256_s_f16`/`kv_append_s_f16` (in decode_state_ptx), `opKvAppendS`/`opAttnDecodeSGraph` kv_f16 flag, per-model kv_dtype field + storeKv + reinitCache (drops+re-captures the decode graph). f16==f32 on deterministic prompts for all; chain spec (`--spec-k` ngram) works+lossless. GATED off under f16 (clean error): qwen3 `--tree`/`--eagle` (tree kernels f32-only); gemma3/qwen35 CPU-offload split (host shadow cache is f32). Graph capture is env-broken on the dev box (per-op path validated; f16 graph kernels ptxas-clean but not exercised e2e here). VULKAN DONE 2026-07-17 (gemma3/qwen35 hd256): new SPIR-V `attn_dsplit_gemma_f16` + `kv_store_f16` (reinterpret f32 storage buffer as packed f16), `opAttnDecodeQ35` kv_f16 flag; validated == f32 + faster; qwen3 vulkan rejects f16. CPU DONE 2026-07-17 (all archs): KvCache/PerLayerKvCache pack 2 f16/f32-slot, zero-copy f32 fast path, expand-on-read; validated qwen3+gemma3 == f32. **f16 KV now on ALL backends × ALL archs.** Gated under f16: qwen3 --tree/--eagle (f32 tree kernels), gemma3/qwen35 CPU-offload (host shadow f32). k-quant KV is a future enum arm.
  - (3) gentler kv_cache.growTarget: currently `cur + max(cur/2, grow_step)` (1.5x geometric) → chunky ~GB reallocations when crossing a boundary. Consider a smaller factor or additive step past some size to smooth the jumps (trade a few more realloc events for less peak over-reservation).
  - context: model /home/qt/genai/lmstudio/models/gemma-4-12B-it-heretic-QAT-UD-Q4_K_XL.gguf, cuda backend, tp-gui chat.)
- Vulkan block-quant GEMV is compute-bound (~13-18x slower than zig-cuda): gemma3-12B Q4_K_M decodes ~3 tok/s on vulkan vs ~54 on zig-cuda. Measured root cause: the vulkan block-quant GEMV kernels (gemv_q4_k_t / q5_k_t / q6_k_t in kernels/eltwise.zig) dequantize every weight to f32 and do scalar FMA → SM pegged at 100% while mem-BW sits at ~6% (compute-bound). zig-cuda uses int8 dp4a → memory-bound (~65% BW), i.e. at the physical floor. Ablation: removing the GEMV dequant compute drops vulkan SM util 100%→15%, so the GEMV is the whole remaining bottleneck (attention was fixed 2026-07-17 via attn_dsplit_gemma flash-split, 1→3 tok/s). FIX: rewrite the vulkan block-quant GEMV to use integer dot product (dp4a-equivalent) via VK_KHR_shader_integer_dot_product (OpUDot/OpSDot; the 3090 supports it) — dequant to int8 in-register + integer-dot accumulate so it becomes memory-bound like cuda. Realistic target ~20-40 tok/s. Real kernel project (new SPIR-V GEMV + int8-friendly weight repack + validation), NOT brief. Block-quant on vulkan is a gemma3/qwen35-only path (qwen3 rejects it and uses f8 coopmat), so there's no existing fast reference to copy. A half-measure (vectorize the current dequant, f16 math) might get ~2x but stays compute-bound.